{
  "title": "AI Agent Performance",
  "uid": "ai-agent-performance",
  "tags": ["ai-agent", "performance"],
  "schemaVersion": 38,
  "version": 1,
  "time": { "from": "now-6h", "to": "now" },
  "panels": [
    {
      "type": "timeseries",
      "title": "Workflow Execution Duration (p95)",
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 0},
      "targets": [
        {"expr": "histogram_quantile(0.95, sum(rate(n8n_workflow_execution_duration_seconds_bucket[5m])) by (le))", "legendFormat": "p95"}
      ]
    },
    {
      "type": "timeseries",
      "title": "HTTP 5xx Error Rate",
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "targets": [
        {"expr": "rate(http_requests_total{status=~\"5..\"}[5m])", "legendFormat": "5xx rps"}
      ]
    },
    {
      "type": "timeseries",
      "title": "RabbitMQ Queue Depth (example)",
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "targets": [
        {"expr": "sum(rabbitmq_queue_messages_ready)"}
      ]
    }
  ]
}
